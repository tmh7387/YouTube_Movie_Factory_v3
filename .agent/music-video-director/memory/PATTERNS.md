# PATTERNS.md — Promoted Reusable Patterns

Lives at: YouTube_Movie_Factory_v3/.agent/music-video-director/memory/
A pattern earns a place here when it proves out across 2+ projects.
Each entry: pattern name, when to apply, why it works, first project that
surfaced it. This file replaces the old "edit SKILL.md Learned Patterns"
mechanism — the skill file is a read-only cache at runtime. When this file
accumulates 5+ stable patterns, regenerate the skill package so they ship
inside the skill itself.

<!-- No patterns promoted yet. Populate via the Feedback Loop. -->
