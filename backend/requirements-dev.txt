-r requirements.txt

# Test + verification tooling. `make verify` installs this, not requirements.txt alone.
pytest>=8.0
pytest-asyncio>=0.24

# alembic/env.py imports this at module scope; it was missing from requirements.txt,
# so `alembic upgrade head` failed on a clean install with ModuleNotFoundError.
python-dotenv>=1.0
