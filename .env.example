# YouTube Movie Factory v3
# Environment Variable Template

# --- Database ---
# The pooled connection URL for the FastAPI app to connect to Neon
DATABASE_URL=postgresql+psycopg://<user>:<password>@<endpoint>.neon.tech/<dbname>?sslmode=require
# The direct connection URL for Alembic migrations
DATABASE_URL_DIRECT=postgresql+psycopg://<user>:<password>@<endpoint>.neon.tech/<dbname>?sslmode=require

# --- Task Queue & Broker ---
REDIS_URL=redis://localhost:6379/0

# --- Core API Keys ---
# Unified gateway key for image/video/music generation and Gemini scoring
COMETAPI_API_KEY=your_cometapi_key_here
# Direct SDK key for Creative Briefs, Image Prompts, Animation Direction, SEO
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# --- YouTube API ---
# OAuth2 Client ID from Google Cloud Console
YOUTUBE_CLIENT_ID=your_youtube_client_id
YOUTUBE_CLIENT_SECRET=your_youtube_client_secret
YOUTUBE_REDIRECT_URI=http://localhost:8000/api/youtube/callback

# --- Application Settings ---
# Default models based on CometAPI reference
DEFAULT_IMAGE_MODEL=nanobananapro     # Options: nanobananapro, seedream4k
DEFAULT_VIDEO_MODEL=kling3           # Options: kling3, seedance, wan26

# Local storage for intermediate generation files
JOB_FILES_DIR=./jobs

# FastAPI Secret Key (generate via: openssl rand -hex 32)
SECRET_KEY=your_openssl_generated_secret_key

# Celery Performance
CELERY_CONCURRENCY=8
